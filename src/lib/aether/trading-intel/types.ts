export const TRADING_INTEL_VERSION = "ti-0.1.0";
export type Timeframe = "5m" | "15m" | "1h" | "4h" | "1D";
export type Availability = "AVAILABLE" | "UNAVAILABLE" | "INSUFFICIENT" | "STALE";
export type Bias = "bullish" | "bearish" | "neutral";
export type IntelDecision = "ENTER" | "WAIT" | "REJECT";
export type Ohlcv = { t: number; o: number; h: number; l: number; c: number; v: number | null };
export type IndicatorName = "RSI" | "MACD" | "EMA" | "SMA" | "ATR" | "BB" | "VWAP" | "ADX" | "OBV";
export type IndicatorReading = { name: IndicatorName; timeframe: Timeframe; timestamp: string; window: number; value: number | null; extras: Record<string, number | null>; freshness: Availability; reason: string; version: string; source: string };
export type StructureEvent = "HH" | "HL" | "LH" | "LL" | "BOS" | "CHOCH" | "TREND" | "CONSOLIDATION" | "RANGE" | "BREAKOUT" | "FAILED_BREAKOUT" | "REJECTION" | "EXHAUSTION" | "VOL_EXPANSION" | "VOL_CONTRACTION";
export type MarketStructureState = { timeframe: Timeframe; timestamp: string; bias: Bias; regime: "trend" | "range" | "consolidation" | "unknown"; events: Array<{ kind: StructureEvent; price: number; at: string; note: string }>; lastSwingHigh: { price: number; t: number } | null; lastSwingLow: { price: number; t: number } | null; freshness: Availability };
export type SupportResistanceLevel = { level: number; timeframe: Timeframe; kind: "support" | "resistance" | "vwap"; strength: number; touches: number; distancePct: number; freshness: Availability; confidence: number; evidence: string };
export type TimeframeSlice = { timeframe: Timeframe; bias: Bias | "UNAVAILABLE"; momentum: Bias | "UNAVAILABLE"; structure: Bias | "UNAVAILABLE"; volatility: "expansion" | "contraction" | "normal" | "UNAVAILABLE"; available: boolean };
export type MultiTimeframeState = { slices: TimeframeSlice[]; trendAlignment: "aligned_bull" | "aligned_bear" | "conflict" | "insufficient"; momentumAlignment: "aligned_bull" | "aligned_bear" | "conflict" | "insufficient"; note: string };
export type FlowState = { volume: number | null; volumeChangePct: number | null; volumeAvailability: Availability; spreadBps: number | null; spreadAvailability: Availability; orderBookImbalance: number | null; orderBookDepthUsd: number | null; bookAvailability: Availability; openInterest: number | null; oiAvailability: Availability; fundingPct: number | null; fundingAvailability: Availability; liquidationsUsd: number | null; liquidationsAvailability: Availability; cvd: number | null; cvdAvailability: Availability; aggressiveBias: Bias | "UNAVAILABLE" };
export type EvidenceItem = { side: "bullish" | "bearish" | "neutral" | "conflict"; source: string; weight: number; detail: string };
export type ConflictState = { bullish: EvidenceItem[]; bearish: EvidenceItem[]; neutral: EvidenceItem[]; conflicting: EvidenceItem[]; conflictScore: number; confidence: number; reason: string };
export type TradingIntelContext = { assetId: string; symbol: string; decisionTimestamp: string; candlesByTf: Partial<Record<Timeframe, Ohlcv[]>>; proposedSide?: "buy" | "sell" | null; volume24hUsd: number | null; liquidityUsd: number | null; spreadBps: number | null; orderBookImbalance: number | null; orderBookDepthUsd: number | null; openInterest: number | null; fundingPct: number | null; liquidationsUsd: number | null; cvd: number | null; eventRisk: Bias | "UNAVAILABLE"; socialBias: Bias | "UNAVAILABLE"; polymarketBias: Bias | "UNAVAILABLE"; hardRiskBlocks: string[]; capitalProfile: "research" | "realistic" | null };
export type TradingIntelligence = { version: string; symbol: string; assetId: string; timestamp: string; decision: IntelDecision; confidence: number; indicators: IndicatorReading[]; structure: MarketStructureState | null; levels: SupportResistanceLevel[]; mtf: MultiTimeframeState; flow: FlowState; conflict: ConflictState; reasons: string[]; technicalEvidence: EvidenceItem[]; marketStructureSummary: string; volumeState: string; liquidityState: string; eventState: string; socialState: string; polymarketState: string; riskState: string; lookaheadClean: boolean; strategyVersion: string };
export type IndicatorAblation = { name: string; baselineExpectancy: number | null; withIndicatorExpectancy: number | null; delta: number | null; n: number; verdict: "ADDS_INFORMATION" | "NO_USEFUL_VALUE" | "HARMS" | "INSUFFICIENT EVIDENCE" };
